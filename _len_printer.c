#include "main.h"
/**
 * _len_printer -  print the string and its length
 * @str: the string
 * Return: the count of the string
 */
int _len_printer(char *str)
{
	int len;

	for (len = 0; str[len] != '\0'; len++)
	return (len);
}

